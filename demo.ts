import { Router } from "express";import { PrismaClient } from "@prisma/client";const prisma=new PrismaClient();const router=Router();router.get("/sample", async (_req,res)=>{ const classes = [{id:1,name:"6A"},{id:2,name:"7B"}]; const timetable = [{id:1,day:"MONDAY",start:"09:00",end:"10:00",classId:1,subject:"Math"}]; const transport = [{id:1,vehicleNo:"BUS-01",route:"North Route"}]; res.json({ classes, timetable, transport });});router.post("/mock-whatsapp", async (req,res)=>{ const { to, message } = req.body; // mock behavior: just log and respond success console.log("MOCK WhatsApp ->", to, message); return res.json({ ok:true, message:"(mock) Message sent" }); }); export default router;