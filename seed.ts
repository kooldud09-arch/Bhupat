import { PrismaClient } from "@prisma/client";import bcrypt from "bcryptjs";const prisma=new PrismaClient();async function main(){ const pwd = process.env.ADMIN_DEFAULT_PASSWORD || "password123"; const hashed = await bcrypt.hash(pwd, 10); const users = [ {name:"Admin", email:"admin@schoolrjn.in", password:hashed, role:"ADMIN"}, {name:"Teacher", email:"teacher@schoolrjn.in", password:hashed, role:"TEACHER"}, {name:"Parent", email:"parent@schoolrjn.in", password:hashed, role:"PARENT"}, {name:"Student", email:"student@schoolrjn.in", password:hashed, role:"STUDENT"} ]; for(const u of users){ const exists = await prisma.user.findUnique({ where: { email: u.email } }); if(!exists) await prisma.user.create({ data: u }); } console.log("Seeded demo users"); } main().catch(e=>{ console.error(e); process.exit(1); }).finally(()=>prisma.$disconnect());